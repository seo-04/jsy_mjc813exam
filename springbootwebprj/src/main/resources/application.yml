spring:
  application:
    name: springbootwebprj
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8	# ?????? URL
    username: springboot_user
    password: springboot#
  mustache:
    suffix: .html
    servlet:
      cache: false
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.mjc813.springbootwebprj
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8989


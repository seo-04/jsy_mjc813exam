spring:
  application:
    name: food_web
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://localhost:3306/food_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8	# ?????? URL
    username: food_user
    password: food_qwer#
  mustache:
    suffix: .html
    servlet:
      cache: false
  jpa:
#    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.mjc813.food_web
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8866


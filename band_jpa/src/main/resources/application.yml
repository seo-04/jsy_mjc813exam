spring:
  application:
    name: band_jpa
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/band_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8	# ?????? URL
    username: band_user
    password: band_4321@
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8385
